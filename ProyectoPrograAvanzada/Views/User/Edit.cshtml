@using ProyectoPrograAvanzada.Models
@model Usuario

@{
    ViewBag.Title = "Editar Usuario: " + Model.nombre;
    // Se asume que el controlador (UserController.Edit GET) pasa una lista de Roles
    // en un ViewBag, ejemplo: ViewBag.Roles = _usuarioService.GetAllRoles();
    IEnumerable<Rol> rolesList = ViewBag.Roles as IEnumerable<Rol>;
}

<h2>@ViewBag.Title</h2>
<p class="lead">Modificación de perfil y privilegios.</p>
<hr />

@using (Html.BeginForm("Edit", "User", FormMethod.Post, new { @class = "form-horizontal" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })

        @* CAMPO OCULTO: ID del Usuario (Obligatorio para la edición) *@
        @Html.HiddenFor(model => model.idUsuario)
        @Html.HiddenFor(model => model.contrasena) @* No editamos la contraseña aquí *@
        @Html.HiddenFor(model => model.email) @* El email no debe cambiarse fácilmente *@

        <div class="form-group">
            @Html.LabelFor(model => model.nombre, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.nombre, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.nombre, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.email, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <p class="form-control-static">@Model.email</p> @* Muestra el email, no permite editar *@
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.estado, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <div class="checkbox">
                    @Html.CheckBoxFor(model => model.estado)
                    @Html.ValidationMessageFor(model => model.estado, "", new { @class = "text-danger" })
                    <span class="help-block">Desmarcar para desactivar la cuenta del usuario.</span>
                </div>
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.idRol, "Rol de Usuario", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @* Dropdown para la selección de Rol (RF1) *@
                @if (rolesList != null)
                {
                    @Html.DropDownListFor(
                        model => model.idRol,
                        new SelectList(rolesList, "idRol", "nombreRol", Model.idRol),
                        "Seleccione un Rol",
                        new { @class = "form-control" }
                    )
                }
                else
                {
                    <p class="form-control-static text-danger">Error: Lista de Roles no cargada.</p>
                }
                @Html.ValidationMessageFor(model => model.idRol, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Guardar Cambios" class="btn btn-success" />
                @Html.ActionLink("Cancelar y Volver", "Index", null, new { @class = "btn btn-default" })
            </div>
        </div>
    </div>
}

<hr />
<p>
    @Html.ActionLink("Volver a la Lista", "Index")
</p>